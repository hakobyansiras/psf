# Returns vector of node ids which do not have incoming edges but only outgoing.

Returns vector of node ids which do not have incoming edges but only
outgoing.

## Usage

``` r
determine.input.nodes(pathway)
```

## Arguments

- pathway:

  pathway list object generated with package.
