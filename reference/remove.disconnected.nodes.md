# Remove nodes which do not have any interactions with other nodes

Remove nodes which do not have any interactions with other nodes

## Usage

``` r
remove.disconnected.nodes(g)
```

## Arguments

- g:

  graphNEL graph
