# Returns the general edge type (either activation or inhibition)

Returns the general edge type (either activation or inhibition)

## Usage

``` r
get.edge.type(g, from, to)
```

## Arguments

- g:

  graphNEl graph

- from:

  parent node id

- to:

  child node id
